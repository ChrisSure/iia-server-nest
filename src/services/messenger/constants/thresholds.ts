/**
 * Alarm probability thresholds (in percentage)
 */

/** Threshold below which alarm probability is considered LOW (< 40%) */
export const ALARM_PROBABILITY_LOW_THRESHOLD = 40;

/** Threshold above which alarm probability is considered HIGH (>= 80%) */
export const ALARM_PROBABILITY_HIGH_THRESHOLD = 80;

/** Maximum alarm probability indicating actual alarm (100%) */
export const ALARM_PROBABILITY_MAX = 100;
