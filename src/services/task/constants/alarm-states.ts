/**
 * Alarm state probability values
 */

/** Maximum alarm probability indicating active alarm state (100%) */
export const ALARM_STATE_ACTIVE = 100;

/** Reset alarm probability to zero when alarm is still active (0%) */
export const ALARM_STATE_RESET = 0;
